{
  "name": "web-docker-config-update",
  "version": "1.0.8",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "MIT",
  "volta": {
    "node": "14.19.2"
  },
  "scripts": {
    "test": "echo nothing to test",
    "build": "rm -rf dist/ && mkdir dist && zip -r9 dist/latest.zip . --include schemas\\* --include index.js --include package.json --include package-lock.json --include node_modules\\*"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "fs": "^0.0.1-security",
    "yaml": "^2.3.4"
  }
}
